import React from 'react'

const Header = () => {
  return (
    <header className="Header text-center">
        <h1 className="">CRUD (Create, Read, Update, Delete) app using React and Easy-Peasy Redux</h1>
    </header>
  )
}

export default Header